***Arrays*** are used to store multiple values of the same type in a single variable.
it is declared in two ways using either using keyword var or with :=.


where length is not specified it is inferred, where values are not assigned and length is specified, default values are assigned. an element of an array can be accessed using the index.

