***Slices*** also used to store multiple values of the same type. slices are more powerful and flexible than arrays and unlike arrays length of slices can grow and shrink as seen fit.


with several ways of creating a slice: declaring directly, making one from an array, using the make() function in go

