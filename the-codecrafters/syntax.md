A go file consists of:
* package declaration that is done using the package keyword.
* import packages that gives access to files included in them.
* functions that contains executable code
* statements and expressions which are lines of code separated by ending a line or by a semicolon(;).

